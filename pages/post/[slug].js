import React from 'react'
import {Categories, PostDetail, PostWidget, Author, Comments, CommentsForm} from '../../components/Categories';
import { getPosts, getPostDetails } from '../../services';

const PostDetails = () => {
    return (
        <div>
            PostDetails
        </div>
    )
}

export default PostDetails
